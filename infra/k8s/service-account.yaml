# Example: k8s/service-account.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: django-sa
  annotations:
    # This links the K8s SA to the IAM Role for AWS access
    eks.amazonaws.com/role-arn: arn:aws:iam::<YOUR_ACCOUNT_ID>:role/portfolio-django-role