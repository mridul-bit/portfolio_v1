name: Backend Fargate CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**' # Triggers only on changes to the backend directory

env:
  CONTAINER_NAME: ${{ secrets.CONTAINER_NAME }} # Hardcoded name from the Task Definition
  
jobs:
  backend-deploy:
    name: 'Backend Fargate Deploy'
    runs-on: ubuntu-latest
    environment: Production
    
    permissions: 
      id-token: write
      contents: read
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      # --- 1. AWS Authentication ---
      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.GH_ACTIONS_DEPLOY_ROLE_ARN }} 
          aws-region: ${{ secrets.AWS_REGION }} 
          
      # --- 2. ECR Login ---
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
        
      # --- 3. Build and Push Docker Image ---
      - name: Build and Push Docker Image
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }} 
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG backend/
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image_uri=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
          
      # =========================================================
      # --- 4. NEW LOGIC: Run Database Migrations (Simplified) ---
      # =========================================================
      - name: Run Database Migrations (Isolated Task) 💾
        id: migration-task
        uses: aws-actions/amazon-ecs-run-task@v1
        with:
          task-definition: ${{ secrets.ECS_TASK_DEFINITION_FAMILY }}
          cluster: ${{ secrets.ECS_CLUSTER_NAME }}
          launch-type: FARGATE
          count: 1
          # The image override is handled internally by this action, 
          # which is the complexity we were fighting with the raw CLI.
          image: ${{ steps.build-image.outputs.image_uri }}
          container: ${{ env.CONTAINER_NAME }}
          # The command to override the container's default entrypoint
          command: '["python", "manage.py", "migrate", "--noinput"]' 
          # Network configuration for Fargate (critical for private DB access)
          subnets: ${{ secrets.PRIVATE_SUBNET_ID }}
          security-groups: ${{ secrets.FARGATE_SG_ID }}
          assign-public-ip: DISABLED
          # This action automatically waits for the task to complete
          # and checks the exit code, simplifying steps 5 and 6 of the old logic!
          wait-for-finish: true
          
      # --- 5. Download and Render Task Definition ---
      # Note: This step is now safe because the migration action handles its own temporary TD lifecycle.
      - name: Download and Render Task Definition 
        uses: aws-actions/amazon-ecs-render-task-definition@v1 
        id: render-task-def
        with:
          task-definition: ${{ secrets.ECS_TASK_DEFINITION_FAMILY }}
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build-image.outputs.image_uri }}

      # --- 6. Register new Task Definition ---
      - name: Register new Task Definition 
        id: register-task-def
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }} # Expose AWS region to the run block
        run: |
          # Use jq to clean the rendered task definition (removes read-only fields)
          NEW_TASK_DEF=$(cat ${{ steps.render-task-def.outputs.task-definition }} | jq 'del(.compatibilities, .taskDefinitionArn, .revision, .status, .requiresAttributes, .placementConstraints, .compatibilities)')
          
          NEW_ARN=$(aws ecs register-task-definition \
            --cli-input-json "$NEW_TASK_DEF" \
            --region ${{ env.AWS_REGION }} \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)
            
          echo "task_def_arn=$NEW_ARN" >> $GITHUB_OUTPUT

      # --- 7. Update ECS Service and Wait for Rollout ---
      - name: Update ECS Service and Wait for Rollout ✨
        run: |
          NEW_TASK_ARN=${{ steps.register-task-def.outputs.task_def_arn }}
          
          # Update the service with the new Task ARN and force a new rollout
          aws ecs update-service \
            --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
            --service ${{ secrets.ECS_SERVICE_NAME }} \
            --task-definition $NEW_TASK_ARN \
            --force-new-deployment
            
          # Wait for the rolling update to complete
          aws ecs wait services-stable \
            --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
            --services ${{ secrets.ECS_SERVICE_NAME }}
          
          echo "Fargate Service successfully rolled out to $NEW_TASK_ARN."