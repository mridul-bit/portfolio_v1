
name: Backend Fargate CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**' # Triggers only on changes to the backend directory

env:
  
  CONTAINER_NAME: ${{ secrets.CONTAINER_NAME }} # Hardcoded name from the Task Definition

jobs:
  backend-deploy:
    name: 'Backend Fargate Deploy'
    runs-on: ubuntu-latest
    environment: Production
    
    permissions: 
      id-token: write
      contents: read
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.GH_ACTIONS_DEPLOY_ROLE_ARN }} 
          aws-region: ${{ secrets.AWS_REGION }} 
          
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
        
      - name: Build and Push Docker Image
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }} #creates tags
          
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }} 
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG backend/
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image_uri=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
          
      - name: Run Database Migrations (Isolated Task) 💾
        run: |
          NEW_IMAGE_URI=${{ steps.build-image.outputs.image_uri }}
          CONTAINER_NAME=${{ env.CONTAINER_NAME }}
          
          # Construct the full override string first using a shell variable
          OVERRIDE_STRING="name=$CONTAINER_NAME,image=$NEW_IMAGE_URI,command=[\"python\", \"manage.py\", \"migrate\", \"--noinput\"]"

          # 1. Launch migration task
          MIGRATION_TASK_ARN=$(aws ecs run-task \
            --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
            --task-definition ${{ secrets.ECS_TASK_DEFINITION_FAMILY }} \
            --launch-type FARGATE \
            --override-container "$OVERRIDE_STRING" \
            --network-configuration "awsvpcConfiguration={subnets=[${{ secrets.PRIVATE_SUBNET_ID }}],securityGroups=[${{ secrets.FARGATE_SG_ID }}],assignPublicIp=DISABLED}" \
            --query 'tasks[0].taskArn' \
            --output text)

          echo "Launched migration task: $MIGRATION_TASK_ARN"
          aws ecs wait tasks-stopped \
            --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
            --tasks $MIGRATION_TASK_ARN
            
          # 3. Check the exit code for the most robust validation
          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
            --tasks $MIGRATION_TASK_ARN \
            --query 'tasks[0].containers[0].exitCode' \
            --output text)

          if [ "$EXIT_CODE" != "0" ]; then
            echo "::error::Database migration failed (Exit Code: $EXIT_CODE). Check task logs."
            exit 1
          fi
          
          echo "Database migration complete."

      - name: Download and Render Task Definition 
        uses: aws-actions/amazon-ecs-render-task-definition@v1 #creates bluprint of task def
        id: render-task-def
        with:
          task-definition: ${{ secrets.ECS_TASK_DEFINITION_FAMILY }}
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build-image.outputs.image_uri }}

      - name: Register new Task Definition 
        id: register-task-def
        run: |
          # Use jq to clean the rendered task definition (necessary when using CLI)
          NEW_TASK_DEF=$(cat ${{ steps.render-task-def.outputs.task-definition }} | jq 'del(.compatibilities, .taskDefinitionArn, .revision, .status, .requiresAttributes, .placementConstraints)')
          
          NEW_ARN=$(aws ecs register-task-definition \
            --cli-input-json "$NEW_TASK_DEF" \
            --region ${{ env.AWS_REGION }} \
            --query 'taskDefinition.taskDefinitionArn' \
            --output text)
            
          echo "task_def_arn=$NEW_ARN" >> $GITHUB_OUTPUT

      - name: Update ECS Service and Wait for Rollout 
        run: |
          NEW_TASK_ARN=${{ steps.register-task-def.outputs.task_def_arn }}
          
          # Update the service with the new Task ARN and force a new rollout
          aws ecs update-service \
            --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
            --service ${{ secrets.ECS_SERVICE_NAME }} \
            --task-definition $NEW_TASK_ARN \
            --force-new-deployment
            
          # Wait for the rolling update to complete
          aws ecs wait services-stable \
            --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
            --services ${{ secrets.ECS_SERVICE_NAME }}
          
          echo "Fargate Service successfully rolled out to $NEW_TASK_ARN."